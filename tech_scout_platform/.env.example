# Optional override for the FastAPI RAG backend.
# Local development defaults to http://localhost:8000 when unset.
# Production can leave this unset if the frontend is served behind the same origin as the API.
VITE_RAG_API_BASE_URL=http://localhost:8000